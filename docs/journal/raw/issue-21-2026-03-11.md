# 2026-03-11 issue 21 execution

## Goal

- Implement GitHub issue `#21`: DM lookup client + DTOs.

## Constraints

- Work from updated `main` in a fresh git worktree.
- Keep scope narrow to `dmguard/x_dm.py`, tests, and honest backlog updates.
- Use Pydantic v2 DTOs named `DMEvent` and `MediaItem`.
- No backward-compatibility aliases without Ori's approval.

## Issue details

- Function: `fetch_dm_event(client: XClient, event_id: str) -> DMEvent`
- Endpoint: `GET /2/dm_events/{event_id}`
- Query params:
  - `event_fields=attachments,created_at,dm_conversation_id,sender_id,text`
  - `expansions=attachments.media_keys,sender_id`
  - `media.fields=type,url,preview_image_url,variants`
- Tests required:
  - request construction
  - response parsing
  - empty or missing attachments
  - `XApiError` propagation

## Planned branch

- Branch: `feat/x-dm-lookup-issue-21`
- Worktree: `.worktrees/feat-x-dm-lookup-issue-21`
