# Issue 40 memory - 2026-03-12

## Scope

- Implement GitHub issue `#40` (`Admin CLI`) on a dedicated feature branch/worktree.
- User-approved plan assumptions:
  - Implement issue `#40` now against current repo reality.
  - `allowlist add` must require `--source-event-id`.
  - `readycheck` is a local readiness probe, not a live process heartbeat.

## Current repo facts

- Existing CLI lives in `dmguard/cli.py` and already handles `setup`, `reset`, `warmup`, and `status`.
- Sender-state tables already exist in schema:
  - `allowed_senders`
  - `blocked_senders`
  - `block_failed_senders`
- Repository helpers already exist for inserting/querying sender state in `dmguard/repo_senders.py`.
- `allowed_senders.source_event_id` is `NOT NULL`, so admin allowlist entries need an explicit source event id.
- The only materially missing command contract is `dmguard selftest`; issue `#27` defines its flags and behavior.

## Implementation guardrails

- Use TDD: add failing CLI tests first, then implement the smallest change set.
- Keep changes local to CLI and sender repository helpers.
- Do not change schema for this issue.
- Update `issues_todo.md` only if issue `#40` is fully complete and verified.
- Update `todo.md` only if a matching checklist item is genuinely verified in this branch.

## Likely touched files

- `dmguard/cli.py`
- `dmguard/repo_senders.py`
- `tests/test_cli.py`
- Possibly a sender-repository test file if delete helper coverage is needed
- `issues_todo.md`
- `todo.md` only if truly warranted
