# 2026-03-12 issue 23 execution

## Goal

- Implement GitHub issue `#23`: media download plus temp file lifecycle.

## Current facts

- User wants the previously proposed plan executed.
- Issue `#22` is closed and PR `#82` is merged.
- `main` currently regresses the merged `#22` behavior in `dmguard/media_dispatch.py`.
- The regression comes from commit `e0bb90c` (`fix: added guard pattern to media_dispatch`), which logs unsupported media but still appends it.
- Targeted baseline failure on `main`: `pytest tests/test_media_dispatch.py -q`.

## Execution decision

- Fold the `media_dispatch` regression fix into the issue `#23` branch before implementing download support.
- Keep scope narrow to the regression fix, `dmguard/media_download.py`, tests, and honest backlog updates.

## Expected branch setup

- Base branch: `main`
- Planned feature branch: `feat/media-download-issue-23`
- Planned worktree: `.worktrees/feat-media-download-issue-23`
