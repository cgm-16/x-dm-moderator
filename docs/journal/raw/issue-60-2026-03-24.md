# Issue 60 Memory

- Date: 2026-03-24
- Issue: `#60 bug(x_client): harden 429 fallback and refresh-ready auth handling`
- Planned branch: `fix/x-client-issue-60-hardening`
- Planned worktree: `.worktrees/fix-x-client-issue-60-hardening`
- Scope:
  - make `_parse_retry_after()` use a positive fallback for missing or invalid values
  - stop freezing `Authorization` in `XClient.__init__`
  - apply bearer auth per request from `SecretStore`
  - keep the existing 401 refresh flow, but do not mutate client default headers
- Test focus:
  - invalid and missing `Retry-After`
  - request-time auth header behavior
  - refreshed token applied on retry
- Tracking files:
  - `todo.md` and `issues_todo.md` do not currently list issue `#60`
