# 2026-03-15 issue 42 execution memory

## Scope decision

- Ori chose to absorb the smallest runtime gap needed for issue `#42` instead of keeping it test-only.

## Repo facts discovered before implementation

- Local repo root was on `main` and stale relative to `origin/main`.
- GitHub issue `#41` is closed and PR `#106` is merged into `main`.
- Local `issues_todo.md` is stale because it still shows `#41` as not done.
- Local `main` still had the pre-`#41` placeholder dispatch in `dmguard/app.py`.

## Runtime gap behind issue 42

- Even on the merged `feat/pipeline-wiring-issue-41` branch, `dmguard/moderator.py` still had a placeholder `_attempt_block_sender()` that never persisted `blocked_senders`.
- Issue `#42` requires the unsafe-media acceptance scenario to assert a real `blocked_senders` row, so runtime code must be completed in the same branch.

## Planned implementation shape

- Preflight sync `main`, then create `feat/e2e-issue-42-acceptance-tests` in a new worktree.
- Add a test-only seam to `create_app()` for worker poll interval, keeping the default at 5 seconds.
- Extend `XClient` with `post()`.
- Implement DM block attempt and sender-state persistence in `moderator.py`.
- Add `tests/test_e2e_acceptance.py` with the six issue-defined scenarios.
- Update `issues_todo.md` to reflect `#41` and `#42` only when verified.
