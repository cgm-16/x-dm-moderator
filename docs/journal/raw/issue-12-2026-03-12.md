# 2026-03-12 issue 12 execution

- Goal: implement worker loop and FastAPI lifespan wiring for issue #12.
- Selected issue: #12 Worker loop + FastAPI lifespan.
- Constraints: smallest change, TDD first, honest backlog updates only after verification.
- Current repo state before preflight:
  - branch: `main`
  - root status: `?? .worktrees/`
  - existing feature worktrees for issues `#18`, `#22`, `#35`, and prior recovery work
- Root-cause hypothesis: `create_app()` bootstraps schema and stale-job recovery on startup, but no background worker task is started, so queued jobs never process after enqueue.
- Chosen defaults for this issue:
  - placeholder dispatch logs the claimed job
  - worker marks successful jobs `done`
  - dispatch failure re-queues with retry backoff
  - retry mapping uses `attempt - 1` when calling `schedule_retry()`
  - `/health` aggregation stays out of scope for issue `#13`
